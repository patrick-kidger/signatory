#ifndef TORCHTEST_SRC_EXTENSION_HPP
#define TORCHTEST_SRC_EXTENSION_HPP

// They use #pragma once, but for safe keeping we'll use guards insteads.
#include <torch/extension.h>

#endif //TORCHTEST_SRC_EXTENSION_HPP
